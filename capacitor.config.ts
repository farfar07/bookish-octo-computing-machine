import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bookish-octo-computing-machine',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
